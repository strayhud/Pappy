<div id="main_area">
    
    <h1>Opportunity Tracker</h1>

    <table id="index_table">
        <tr>
            <th>ACCOUNT</th>
            <th>OPPORTUNITY</th>
            <th>DATE</th>
        </tr>

        <% @qforms.each do |qform| %>
        <tr>
            <td><%= link_to qform.customer, qform %></td>
            <td><%= link_to qform.purpose, qform %></td>
            <td><%= link_to qform.created_at, qform %></td>
        </tr>
        <% end %>
    </table>

    <br />
    <a href="dms.html"><img border="0" style="float: right; margin-bottom: -10px" src="CustomerFacing_RPMOverview_files/home.png" alt="Home"/></a>

    <%= link_to 'New Request', new_qform_path, :class=>"link_button"  %>
</div>